services:
  scraper:
    build: .
    env_file: .env
    volumes:
      - ./runtime:/app/runtime
      - ./config:/app/config

# Postgres is hosted on Supabase — no local postgres service needed.
# To run locally instead, uncomment below and update DATABASE_URL in .env:
#
#  postgres:
#    image: postgres:16
#    environment:
#      POSTGRES_DB: fb_scraper
#      POSTGRES_USER: postgres
#      POSTGRES_PASSWORD: postgres
#    ports:
#      - "5432:5432"
#    volumes:
#      - pgdata:/var/lib/postgresql/data
#
# volumes:
#   pgdata:
